# Docker Environment Configuration
# Copy this file to .env for Docker Compose setup
# This is an environment variables file, not a shell script

# Node Environment
NODE_ENV=development

# Database Configuration
DB_USER=gobeyondfit
DB_PASSWORD=gobeyondfit123
DB_NAME=gobeyondfit_db
DB_PORT=5432

# Backend Configuration
BACKEND_PORT=3000
PORT=3000
CORS_ORIGIN=http://localhost:3001

# Frontend Configuration
FRONTEND_PORT=3001
NEXT_PUBLIC_API_URL=http://localhost:3000

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_change_in_production

# Email Configuration (SMTP)
# For development, use Ethereal (free test email service)
# Sign up at https://ethereal.email to get credentials
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=frederick.homenick55@ethereal.email
SMTP_PASS=wkymjYXuS6UhSuPnM2
SMTP_FROM=noreply@gobeyondfit.com
SMTP_SECURE=false

# Frontend URLs
FRONTEND_URL=http://localhost:3001
